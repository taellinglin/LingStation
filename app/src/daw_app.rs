use super::*;

pub(super) struct DawApp {
    pub(super) project_name: String,
    pub(super) project_path: String,
    pub(super) metadata_artist: String,
    pub(super) metadata_title: String,
    pub(super) metadata_album: String,
    pub(super) metadata_genre: String,
    pub(super) metadata_year: String,
    pub(super) metadata_comment: String,
    pub(super) project_key: Option<u8>,
    pub(super) project_key_minor: bool,
    pub(super) tracks: Vec<Track>,
    pub(super) selected_clip: Option<usize>,
    pub(super) selected_track: Option<usize>,
    pub(super) playhead_beats: f32,
    pub(super) last_frame_time: Option<f64>,
    pub(super) audio_running: bool,
    pub(super) audio_stream: Option<cpal::Stream>,
    pub(super) midi_conns: Vec<MidiInputConnection<()>>,
    pub(super) audio_stop: Arc<AtomicBool>,
    pub(super) audio_callback_active: Arc<AtomicUsize>,
    pub(super) playback_panic: Arc<AtomicBool>,
    pub(super) playback_fade_in: Arc<AtomicBool>,
    pub(super) midi_freq_bits: Arc<AtomicU32>,
    pub(super) midi_gate: Arc<AtomicBool>,
    pub(super) tempo_bits: Arc<AtomicU32>,
    pub(super) transport_samples: Arc<AtomicU64>,
    pub(super) master_peak_bits: Arc<AtomicU32>,
    pub(super) master_peak_display: f32,
    pub(super) master_settings: Arc<Mutex<MasterCompSettings>>,
    pub(super) master_comp_state: Arc<Mutex<MasterCompState>>,
    pub(super) last_output_channels: usize,
    pub(super) track_audio: Vec<TrackAudioState>,
    pub(super) track_mix: Arc<Mutex<Vec<TrackMixState>>>,
    pub(super) node_activity_rt: Arc<Mutex<Vec<TrackNodeActivity>>>,
    pub(super) selected_track_index: Arc<AtomicUsize>,
    pub(super) midi_learn: Arc<Mutex<Option<(usize, u32)>>>,
    pub(super) rename_buffer: String,
    pub(super) rename_clip_buffer: String,
    pub(super) rename_clip_target: Option<(usize, usize)>,
    pub(super) show_rename_track: bool,
    pub(super) show_rename_clip: bool,
    pub(super) project_name_buffer: String,
    pub(super) show_rename_project: bool,
    pub(super) show_settings: bool,
    pub(super) show_project_info: bool,
    pub(super) show_metadata: bool,
    pub(super) show_help_about: bool,
    pub(super) show_help_license: bool,
    pub(super) show_help_shortcuts: bool,
    pub(super) show_help_general: bool,
    pub(super) show_sidebar: bool,
    pub(super) show_mixer: bool,
    pub(super) show_transport: bool,
    pub(super) main_tab: MainTab,
    pub(super) settings_tab: SettingsTab,
    pub(super) show_hitboxes: bool,
    pub(super) tempo_bpm: f32,
    pub(super) arranger_pan: egui::Vec2,
    pub(super) arranger_zoom: f32,
    pub(super) piano_pan: egui::Vec2,
    pub(super) piano_zoom_x: f32,
    pub(super) piano_zoom_y: f32,
    pub(super) piano_note_len: f32,
    pub(super) piano_snap: f32,
    pub(super) piano_roll_hovered: bool,
    pub(super) piano_key_down: Option<u8>,
    pub(super) piano_lane_mode: PianoLaneMode,
    pub(super) piano_cc: u8,
    pub(super) import_path: String,
    pub(super) export_path: String,
    pub(super) status: String,
    pub(super) license_identifier: String,
    pub(super) license_password: String,
    pub(super) license_serial: String,
    pub(super) license_device_label: String,
    pub(super) license_status: String,
    pub(super) license_job: Option<LicenseJob>,
    pub(super) last_ui_param_change: Option<(u32, f32)>,
    pub(super) preset_name_buffer: String,
    pub(super) startup_stream: Option<OutputStream>,
    pub(super) startup_sink: Option<Sink>,
    pub(super) settings: SettingsState,
    pub(super) settings_path: String,
    pub(super) show_plugin_picker: bool,
    pub(super) show_plugin_ui: bool,
    pub(super) plugin_ui_target: Option<PluginUiTarget>,
    pub(super) project_dirty: bool,
    pub(super) last_autosave_at: Option<std::time::Instant>,
    pub(super) show_close_confirm: bool,
    pub(super) pending_project_action: Option<ProjectAction>,
    pub(super) pending_exit: bool,
    pub(super) exit_confirmed: bool,
    pub(super) show_render_dialog: bool,
    pub(super) render_format: RenderFormat,
    pub(super) render_sample_rate: u32,
    pub(super) render_wav_bit_depth: RenderWavBitDepth,
    pub(super) render_bitrate: u32,
    pub(super) render_split_tracks: bool,
    pub(super) render_target_dir: Option<PathBuf>,
    pub(super) render_progress: Option<(u64, u64)>,
    pub(super) render_job: Option<RenderJob>,
    pub(super) render_range_start: f32,
    pub(super) render_range_end: f32,
    pub(super) render_tail_mode: RenderTailMode,
    pub(super) render_release_seconds: f32,
    pub(super) record_audio: bool,
    pub(super) record_midi: bool,
    pub(super) record_automation: bool,
    pub(super) record_performance: bool,
    pub(super) is_recording: bool,
    pub(super) record_started_audio: bool,
    pub(super) recording: Arc<Mutex<RecordingBuffers>>,
    pub(super) audio_input_stream: Option<cpal::Stream>,
    pub(super) plugin_candidates: Vec<PluginCandidate>,
    pub(super) plugin_search: String,
    pub(super) plugin_target: Option<PluginTarget>,
    pub(super) show_midi_import: bool,
    pub(super) midi_import_state: Option<MidiImportState>,
    pub(super) undo_stack: Vec<UndoState>,
    pub(super) redo_stack: Vec<UndoState>,
    pub(super) clip_drag: Option<ClipDragState>,
    pub(super) track_drag: Option<TrackDragState>,
    pub(super) arranger_tool: ArrangerTool,
    pub(super) arranger_select_start: Option<egui::Pos2>,
    pub(super) arranger_select_add: bool,
    pub(super) arranger_draw: Option<ArrangerDrawState>,
    pub(super) arranger_slice_drag: Option<ArrangerSliceDragState>,
    pub(super) _clip_clipboard: Option<Clip>,
    pub(super) waveform_cache: RefCell<HashMap<Arc<str>, Vec<f32>>>,
    pub(super) waveform_color_cache: RefCell<HashMap<Arc<str>, Vec<[f32; 3]>>>,
    pub(super) waveform_len_seconds_cache: RefCell<HashMap<Arc<str>, f32>>,
    pub(super) waveform_cache_order: RefCell<VecDeque<Arc<str>>>,
    pub(super) waveform_color_cache_order: RefCell<VecDeque<Arc<str>>>,
    pub(super) waveform_len_seconds_cache_order: RefCell<VecDeque<Arc<str>>>,
    pub(super) audio_clip_cache: Arc<Mutex<AudioClipCache>>,
    pub(super) audio_clip_timeline: Arc<Mutex<Vec<AudioClipRender>>>,
    pub(super) analysis_sender: Option<Sender<AudioAnalysisRequest>>,
    pub(super) analysis_receiver: Option<Receiver<AudioAnalysisResult>>,
    pub(super) analysis_pending: HashSet<usize>,
    pub(super) audio_preview_stream: Option<OutputStream>,
    pub(super) audio_preview_sink: Option<Sink>,
    pub(super) audio_preview_loop: bool,
    pub(super) audio_preview_clip_id: Option<usize>,
    pub(super) audio_stats: Arc<AudioRuntimeStats>,
    pub(super) ui_frame_last_ms: f32,
    pub(super) ui_frame_max_ms: f32,
    pub(super) ui_arranger_last_ms: f32,
    pub(super) ui_arranger_max_ms: f32,
    pub(super) buffer_override: Option<u32>,
    pub(super) adaptive_restart_requested: Arc<AtomicBool>,
    pub(super) adaptive_buffer_size: Arc<AtomicU32>,
    pub(super) adaptive_restart_pending: bool,
    pub(super) last_overrun: Arc<AtomicBool>,
    pub(super) piano_drag: Option<PianoDragState>,
    pub(super) piano_scale_drag: Option<PianoScaleDragState>,
    pub(super) piano_zoom_drag: Option<PianoZoomDragState>,
    pub(super) piano_tool: PianoTool,
    pub(super) arranger_snap_beats: f32,
    pub(super) piano_selected: HashSet<usize>,
    pub(super) piano_marquee_start: Option<egui::Pos2>,
    pub(super) piano_marquee_add: bool,
    pub(super) piano_cc_drag: Option<usize>,
    pub(super) piano_roll_rect: Option<egui::Rect>,
    pub(super) piano_roll_panel_height: f32,
    pub(super) piano_focus_beats: Option<f32>,
    pub(super) selected_clips: HashSet<usize>,
    pub(super) plugin_ui: Option<PluginUiHost>,
    pub(super) plugin_ui_hidden: bool,
    pub(super) plugin_ui_resume_at: Option<std::time::Instant>,
    pub(super) last_params_track: Option<usize>,
    pub(super) last_viewport_maximized: Option<bool>,
    pub(super) last_viewport_rect: Option<egui::Rect>,
    pub(super) pending_startup_maximize: bool,
    pub(super) seen_nonzero_viewport: bool,
    pub(super) pending_viewport_focus: bool,
    pub(super) pending_repaint_frames: u32,
    pub(super) wallpaper_texture: Option<egui::TextureHandle>,
    pub(super) wallpaper_texture_path: String,
    pub(super) fs_expanded: HashSet<String>,
    pub(super) fs_selected: Option<String>,
    pub(super) browser_expanded: HashSet<String>,
    pub(super) browser_selected: Option<String>,
    pub(super) sidebar_tab: SidebarTab,
    pub(super) fs_drag: Option<FsDragState>,
    pub(super) loop_start_beats: Option<f32>,
    pub(super) loop_end_beats: Option<f32>,
    pub(super) loop_start_samples: Arc<AtomicU64>,
    pub(super) loop_end_samples: Arc<AtomicU64>,
    pub(super) orphaned_hosts: Vec<PluginHostHandle>,
    pub(super) automation_active: Option<(usize, usize)>,
    pub(super) automation_rows_expanded: HashSet<usize>,
    pub(super) node_routes: Vec<NodeRouteLink>,
    pub(super) node_routes_rt: Arc<Mutex<Vec<NodeRouteLink>>>,
    pub(super) node_route_kind: NodeRouteKind,
    pub(super) node_route_from_track: usize,
    pub(super) node_route_source_output_pair: usize,
    pub(super) node_route_to_track: usize,
    pub(super) node_route_to_fx: usize,
    pub(super) node_view_pan: egui::Vec2,
    pub(super) node_view_zoom: f32,
    pub(super) node_map_height: f32,
    pub(super) performance_clip_settings: HashMap<usize, PerformanceClipSettings>,
    pub(super) performance_launch_quantize_beats: f32,
    pub(super) performance_selected_clip: Option<usize>,
    pub(super) performance_runtime: Arc<Mutex<Vec<Option<PerformanceRuntimeClip>>>>,
    pub(super) arrangement_playback_enabled: Arc<AtomicBool>,
    pub(super) gm_presets_generated: bool,
}

